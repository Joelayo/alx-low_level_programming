#include <stdio.h>
/**
 * main - main block
 * Description: prints the alphabet in lowercase
 * followed by a new line
 * Return: 0
 */
int main(void)
{
	for (int x = 'a'; x <= 'z' x++)
	{
		putchar(x);
	}
	return (0);
}

